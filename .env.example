# GitHub Configuration
GITHUB_TOKEN=ghp_xxxx           # GitHub PAT with repo access
GITHUB_REPOSITORY=owner/repo    # Repository in owner/repo format

# LLM Provider Configuration
LLM_PROVIDER=openai             # Provider: openai, grok, yandex
LLM_MODEL=gpt-4o-mini           # Model to use (provider-specific)

# Provider API Keys (set the one for your chosen provider)
OPENAI_API_KEY=sk-xxxx          # OpenAI API key (if using openai)
GROK_API_KEY=xai-xxxx           # Grok/xAI API key (if using grok)
YANDEX_API_KEY=xxxx             # YandexGPT API key (if using yandex)
YANDEX_FOLDER_ID=xxxx           # YandexGPT folder ID (if using yandex)

# Agent Configuration
MAX_ITERATIONS=5                # Max code generation attempts
ITERATION_TIMEOUT=600           # Seconds per iteration
